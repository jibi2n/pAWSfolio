# pawsfolio (app)

This folder is the Next.js app for **builds.log**. See the [main README](../README.md) for what it is, setup, and project structure.

```bash
npm install
npm run dev   # http://localhost:3000
```
